#  Matrix
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

numbers = [5, 2, 1, 7, 4, 5]
boolean = 5 in numbers  # check if 5 is in numbers
print(numbers[0:3])  # print subArray of numbers
numbers = [1, 2, 3]
x, y, z = numbers

# tuples (tuples are immutable i.e. once the value is set, cannot change its value)
numbers = (1, 2, 3)
print(numbers[0])
# numbers[0] = 1
x, y, z = numbers  # similar to const {x,y,z} = numbers
